@model List<Category>

<div class="list-group">
    @foreach (var category in Model)
    {
        <a href="@Url.RouteUrl("category", new { categoryId = category.CategoryId })" class="list-group-item list-group-item-action">@category.Name</a>
    }
</div>
